# Paperclip Repo Front Door — AI agent rules

This file is auto-generated by sync-llms-to-repos.sh from the HLT Obsidian
System Maps. Edit the source in Obsidian (not here); this file is regenerated
daily. Source: ~/Documents/Obsidian Vault/OpenClaw/System Maps/

## Read first

Before making changes in this repo, read **`llms.txt`** at the repo root.
It contains the repo-specific preface + the shared HLT ecosystem canon.
For maximum context, read **`llms-full.txt`** (the full System Map bundle).

## How this repo fits

## Repo quickstart

- **What this IS**: Governance plane for agents (HLT fork): companies, goals, budgets, approvals, audit log. Does not execute — coordinates adapters.
- **Live URL**: n/a (local; branch session-2026-04-16-mastra-gateway-adapter-wip)
- **Local run**: `pnpm -r dev`
- **Env vars this repo reads**: DATABASE_URL (separate Supabase), adapter-specific env (CLAUDE_*, CODEX_*, MASTRA_*). Never paste values — resolution via `kb:katailyst-vault-access-pattern`.

## Repo-specific note

You are currently inside **Paperclip**, the Mastra-gateway-adapter lane. Parked on session branch with adapter scaffold complete but unregistered in Paperclip core. Six release-focused GitHub workflows; one of the more CI-mature non-katailyst repos.

### Inspect first in this repo
- adapter scaffold
- session branch state
- release workflows


### Recent significant changes (last 5 commits)

- 3bb8ebdc 2026-04-24 feat(observability): trace Paperclip heartbeat runs with Langfuse
- f5014630 2026-04-24 ci: skip docker publish on fork pushes
- 9b30c0a3 2026-04-24 ci: skip canary publish on fork pushes
- be9f7075 2026-04-24 Merge remote-tracking branch 'origin/master'
- 4abf1372 2026-04-24 docs: refresh llms recent changes

## Behavioral rules

- Treat `llms.txt` and `docs/openclaw-system-maps/` as read-only in this repo.
  Edits must go to the Obsidian source. See
  `~/.openclaw/workspace/system/how-to-edit-sacred-docs.md`.
- Multiple agents may be working at once. Stay inside your scope, do not
  rename/refactor unrelated files, and never `git stash` or switch branches
  unless the operator asked for it.
- Prefer existing functions and utilities over inventing new ones. Check
  Katailyst via MCP (`discover`, `traverse`, `get_entity`) before assuming
  something doesn't exist.
- When a change spans repos (this repo + sidecar + MasteryPublishing etc.),
  flag the cross-repo impact to the operator before starting.
- On ambiguity, prefer asking a focused clarifying question over assumption.

## Ecosystem canon

- **Katailyst** — capability/registry canon + MCP
- **sidecar-system** — content orchestration + projection
- **MasteryPublishing** — canonical `/resources/**` destination
- **Multimedia Mastery** — media generation lane (Cloudinary-primary)
- **Agent-Canvas** — multi-agent command surface

More detail lives in `llms.txt`.
